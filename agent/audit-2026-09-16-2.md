# 稽核紀錄（2026-09-16 第二輪）

使用者要求再次檢查具體程式碼缺陷。本輪由主代理逐行重讀全部核心實作，建立基線後以 TDD 修復三個確認缺陷；未使用子代理。

## 檢查範圍與結論

- 重讀 `internal/model`、`internal/collector`、`internal/store`（store／runtime／maintenance）、`internal/server`（server／notify）、`cmd/server/main.go`、`cmd/agent/main.go` 與 `web/src`（App.vue、Trend、RuleFields、types、main）。
- 資料正確性、權限、鎖順序、離線判定、通知佇列與資料庫搬移路徑未發現新缺陷。
- 記錄但不修：`server.go` `events()` 使用 `time.Now()` 而非 `s.Now()`（與可注入時鐘不一致，行為等價、非缺陷）；`store.go` `Record()` 已有 `sample != nil` 分支，`Tick` 傳 nil 安全。

## 基線（修復前）

- `go test ./... -count=1` 全過；`npm --prefix web test` 15/15。

## 缺陷與 RED／GREEN

| # | 缺陷 | RED 重現 | GREEN 修復 |
| --- | --- | --- | --- |
| A | README 支援 `http://VPS_IP:8080`，非安全環境無 `navigator.clipboard`，點「複製 Token」顯示英文 TypeError | `web/src/App.resilience.test.ts`：原型上移除 clipboard 後點複製，實渑顯示 `Cannot read properties of undefined (reading 'writeText')` | `copyToken` 先試 clipboard API，不可用或失敗退回 `legacyCopy`（隱形 textarea ＋ `document.execCommand("copy")`），仍失敗顯示「無法存取剪貼簿，請手動選取 Token 複製」 |
| B | `api()` 對非 JSON 回應（反向代理 502 純文字）`r.json()` 丢出 SyntaxError 顯示給用戶 | 同檔：502 回 `Bad Gateway` 純文字，實測顯示 `Unexpected token` | `r.json()` 包 try-catch，解析失敗時改顯示 `請求失敗（502）`，成功路徑行為不變 |
| C | `cmd/server/main.go` 背景任務失敗僅 `log.Print("background task failed")`，丟失錯誤細節 | `TestLogTaskFailureIncludesReason`：先因 `undefined: logTaskFailure` 編譯失敗 | 新增 `logTaskFailure(ctx, err)`：僅在 ctx 未取消時輸出 `background task failed: %v`，關閉期間不再輸出雜訊 |

## 驗證

- RED：前端 `App.resilience.test.ts` 3/3 失敗（實證錯誤訊息會直接顯示給用戶）；Go 編譯失敗 `undefined: logTaskFailure`。
- GREEN：`npm --prefix web test` 18/18（原 15 ＋ 新 3）；`go test ./cmd/server -count=1` 通過。
- 完整回歸：`go test ./... -count=1` 七個套件全過、`go vet ./...` 乾淨、`npm --prefix web run build`（vue-tsc ＋ Vite）通過，JS 95.81 kB／gzip 36.40 kB。
- 限制不變：本機無 PostgreSQL 實例與完整 race，待遠端 Linux CI；本輪變更僅在工作區，未提交推送、未變更 Release（v0.1.2 仍為最新正式版，未含本輪修復）。
